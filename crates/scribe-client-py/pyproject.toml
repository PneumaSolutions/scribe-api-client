[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "scribe-client"
version = "0.1.0"
description = "Python bindings for the Scribe document conversion API client"
requires-python = ">=3.9"

[tool.maturin]
module-name = "scribe_client"
